/*
* etwas Kommentar
*/
  package
    test /*blubber*/ {

/**
* und nochne klasse Klasse
*/
public class Test /* blub ber */extends Object {

  public function Test(i :int, p: boolean) {
    this.i = i;
    this.p = p;
  }

  public abstract function m():void;

  public var i :int;
  protected var p :boolean;

  public function m2(x :int, y:Object):boolean {
    some.other.packg.otherClass.prop=true;
    assert(1+1==2.5);
    some.other.packg.otherClass.m(prop);
    // compiler bug retests:
    while (true) switch(true) {};
    for ( ; i<=n; ++i) ;
    var f = function xxx() { return "bar"; };
    return true;
  }

  public override function m3(x :int, y:Object):boolean {
    return true;
  }

  /*public function Test(i :int, p: boolean) {
    // Constructor must be first declaration in class, should be catched by compiler
    this.i = i;
    this.p = p;
  }*/

  public static function global() {
  }

  public static function init() {
    foo(function() { return "bar";});
  }

}}