<?xml version="1.0" encoding="UTF-8"?>
<!---This is a test plugin (not a component!).-->
<extends:Plugin
             xmlns:fx="http://ns.adobe.com/mxml/2009"
             xmlns:extends="ext.*"
             xmlns:cfg="testNamespace.config.*"
             xmlns:exml="http://www.jangaroo.net/exml/0.8"
             xmlns="exml:ext.config">
  <fx:Script><![CDATA[
    import testNamespace.config.testPlugin;

    public native function TestPlugin(config:testNamespace.config.testPlugin = null);
  ]]></fx:Script>
  <fx:Metadata>[DefaultProperty("config")]</fx:Metadata>
  <fx:Declarations>
    <cfg:testPlugin id="config"/>
  </fx:Declarations>

  <plugin/>
</extends:Plugin>
