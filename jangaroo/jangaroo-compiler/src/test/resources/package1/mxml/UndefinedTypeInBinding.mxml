<?xml version="1.0"?>
<ConfigClass
        xmlns:fx="http://ns.adobe.com/mxml/2009"
        items="{[function():*{
          return /* UndefinedType.mxml would raise compiler error, but is only scoped, so all is fine: */ UndefinedType(null);
        }]}"
        xmlns="package1.*"/>
